{
    "input": [
        {
            "type": "text",
            "action": "add",
            "args": {
                "inputDir": "${RULESET_SOURCE_GEOIP_DIR}/"
            }
        }
    ],
    "output": [
        {
            "type": "v2rayGeoIPDat",
            "action": "output",
            "args": {
                "outputDir": "${CURRENT_DIR}",
                "outputName": "${IP_FILE_NAME}"
            }
        }
    ]
}